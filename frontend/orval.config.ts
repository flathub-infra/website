import { InfoObject } from "openapi3-ts/oas30"

module.exports = {
  "flathub-file-transfomer": {
    output: {
      mode: "tags-split",
      workspace: "./src/codegen",
      target: "./",
      schemas: "./model",
      client: "react-query",
      mock: true,
      prettier: true,
      clean: true,
      baseUrl: "${process.env.NEXT_PUBLIC_API_BASE_URI}",
      override: {
        header: (info: InfoObject): String[] => [
          `Generated by orval 🍺`,
          `Do not edit manually.`,
          ...(info.title ? [info.title] : []),
          ...(info.description ? [info.description] : []),
          ...(info.version ? [`OpenAPI spec version: ${info.version}`] : []),
        ],
      },
    },
    input: {
      target: "http://localhost:8000/openapi.json",
    },
  },
  "flathub-pipeline-file-transfomer": {
    output: {
      mode: "tags-split",
      workspace: "./src/codegen-pipeline",
      target: "./",
      schemas: "./model",
      client: "react-query",
      mock: true,
      prettier: true,
      clean: true,
      baseUrl: "https://flathub-vorarbeiter.apps.openshift.gnome.org",
      override: {
        header: (info: InfoObject): String[] => [
          `Generated by orval 🍺`,
          `Do not edit manually.`,
          ...(info.title ? [info.title] : []),
          ...(info.description ? [info.description] : []),
          ...(info.version ? [`OpenAPI spec version: ${info.version}`] : []),
        ],
      },
    },
    input: {
      target:
        "https://flathub-vorarbeiter.apps.openshift.gnome.org/openapi.json",
    },
  },
}
